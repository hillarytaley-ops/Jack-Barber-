# Local dev only — copy to .env and fill in. On live, set these in Vercel (see VERCEL-ENV-LIVE.txt).

ADMIN_PASSWORD=your-local-admin-password

# Jack's Barber Style — client PayID (ABN)
PAYID=16440489587
PAYID_NAME=Jacques Katumbulu
BUSINESS_ABN=16 440 489 587
SITE_URL=http://localhost:3000

# Set true only after Jack confirms GST registration with accountant
GST_REGISTERED=false

# Optional
# RESEND_API_KEY=
# CRON_SECRET=
