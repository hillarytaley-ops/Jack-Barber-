services:
  - type: web
    name: jacks-barber-style
    runtime: node
    plan: free
    buildCommand: echo "No build step"
    startCommand: node server/server.js
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: ADMIN_PASSWORD
        sync: false
